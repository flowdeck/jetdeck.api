export default [
  {
    root: true,
    // env: {
    //   node: true, // Indicate a Node.js environment
    //   // es2021: true, // Adjust as needed for your supported ECMAScript version
    // },
    // extends: [
    //   'eslint:recommended', // Include ESLint's recommended ruleset
    // ],
    // ignorePatterns: ['dist', '.eslintrc.cjs'],
    rules: {
      // Your custom JavaScript rules here (e.g., 'no-unused-vars': 'warn')
      'no-unused-vars': 'warn',
    },
  },
]
